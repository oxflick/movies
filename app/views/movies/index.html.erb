<div class="jumbotron">
<h2> What's the movie about?</h2>
<h3> Explore your favorite or currently showing in theater movies. <br>
Leave reviews, ratings for your fellow movie lover! And VOTE for similar movies. </h3>
</div>
<div class="table-responsive">
<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Title</th>
      <th>Year</th>
      <th>Actor</th>
      <th>Genre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= movie.title %></td>
        <td><%= movie.year %></td>
        <td><%= movie.actor %></td>
        <td><%= movie.genre %></td>
        <td><%= link_to 'Show', movie, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_movie_path(movie), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<br>

<%= link_to 'New Movie', new_movie_path, class: "btn btn-link" %>
